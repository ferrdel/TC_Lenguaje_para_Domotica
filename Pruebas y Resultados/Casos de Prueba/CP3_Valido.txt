si (estado_alarma == activado) {
    encender(luces_emergencia);
    esperar(5);
    apagar(luces_emergencia);
    encender(sirena);
}

repetir (10) {
    esperar(2);
    encender(flash_patio);
    esperar(1);
    apagar(flash_patio);
}